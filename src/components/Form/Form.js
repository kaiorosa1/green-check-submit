import React from 'react';

const Form = () =>{
    return (
        <div>
            <form>
                <input type="text" />
                <button>Send</button>
            </form>
        </div>
    );
}

export default Form;